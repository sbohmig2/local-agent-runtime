# Generate a long random value for the authenticated loopback gateway.
LOCAL_AGENT_RUNTIME_TOKEN=replace-with-at-least-32-random-characters

# Required only by an enabled OpenRouter provider.
OPENROUTER_API_KEY=replace-with-your-openrouter-api-key
